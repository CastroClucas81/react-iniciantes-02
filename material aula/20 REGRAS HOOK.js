import React from "react";

/*
  O QUE SERÁ FEITO?
  regras hooks

  nao posso colocar ele dentro de uma condicao (ex.: if())

  nao posso colocar ele dentro de uma funcao

  nao posso colocar ele dentro de um loop

  ONDE USAR?
    dentro de componentes e dentro de custom hook

*/

const App = () => {
  return <></>;
};

export default App;
